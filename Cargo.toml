[package]
name = "second-stack"
version = "0.2.0"
authors = ["Zac <that3percent@gmail.com>"]
edition = "2018"
description = "A fast slice allocator for short-lived allocations that do not escape the thread."
homepage = "http://www.that3percent.com/second-stack"
repository = "https://github.com/That3Percent/second-stack"
readme = "README.md"
keywords = ["slice", "stack", "memory-management"]
categories = ["memory-management"]
license = "MIT"

[badges]
maintenance = { status = "actively-developed" }

[dev-dependencies]
testdrop = "0.1.2"

[features]
experimental = []